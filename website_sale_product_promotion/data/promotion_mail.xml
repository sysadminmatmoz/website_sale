<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="email_template_product_promotion" model="mail.template">
        <field name="name">Promotion Email</field>
        <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
        <field name="subject">Our promotion for this week : ${object.name}</field>
        <field name="partner_to">${object.partner_id.id}</field>
        <field name="email_to">${object.email|safe}</field>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html">
            <![CDATA[
	          <p>Dear ${(object.name)},<br/>
	          <br/>
		  Good job, you've just created your first e-mail template!<br/>
		  </p>
                  Regards,<br/>
                  ${(object.company_id.name)}
	    ]]>
        </field>
    </record>
</odoo>